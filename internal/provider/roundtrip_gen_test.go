// Code generated by supermetal-inc/terraform-provider-gen@6a2b361, spec v0.1.10. DO NOT EDIT.

package provider

import (
	"reflect"
	"strings"
	"testing"

	"github.com/google/go-cmp/cmp"
	"github.com/hashicorp/terraform-plugin-framework/types"
)

// redactSecrets recursively walks an API struct and clears all string fields
// that contain secret values (identified by the "secret-" prefix from synthetic test values).
// This simulates what the API returns when secrets are redacted.
func redactSecrets(v interface{}) {
	redactValue(reflect.ValueOf(v))
}

func redactValue(v reflect.Value) {
	if !v.IsValid() {
		return
	}

	switch v.Kind() {
	case reflect.Ptr:
		if !v.IsNil() {
			redactValue(v.Elem())
		}
	case reflect.Struct:
		for i := 0; i < v.NumField(); i++ {
			field := v.Field(i)

			// Redact string fields that have the secret prefix
			if field.Kind() == reflect.String && field.CanSet() {
				if strings.HasPrefix(field.String(), "secret-") {
					field.SetString("**REDACTED**")
				}
			} else if field.Kind() == reflect.Ptr && !field.IsNil() {
				elem := field.Elem()
				if elem.Kind() == reflect.String && strings.HasPrefix(elem.String(), "secret-") {
					redacted := "**REDACTED**"
					field.Set(reflect.ValueOf(&redacted))
				} else {
					redactValue(field)
				}
			} else {
				redactValue(field)
			}
		}
	case reflect.Slice:
		for i := 0; i < v.Len(); i++ {
			redactValue(v.Index(i))
		}
	case reflect.Map:
		for _, key := range v.MapKeys() {
			redactValue(v.MapIndex(key))
		}
	case reflect.Interface:
		if !v.IsNil() {
			redactValue(v.Elem())
		}
	}
}

// buildFullBigQuerySinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullBigQuerySinkModel() *BigQuerySinkModel {
	return &BigQuerySinkModel{
		Auth: &BigqueryAuthModel{
			ServiceAccountKey: &BigqueryServiceAccountKeyModel{
				KeyJSON: types.StringValue("secret-key_json"),
			},
		},
		Dataset:             types.StringValue("v-dataset"),
		DisableSchemaPrefix: types.BoolValue(true),
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		ProjectID:     types.StringValue("v-project_id"),
		QueryPriority: types.StringValue("Interactive"),
		WriteMode: &BigqueryWriteModeModel{
			Merge: &BigqueryMergeModel{
				UseTransactions: types.BoolValue(true),
			},
		},
	}
}

// buildFullBigQuerySinkModel_WithMerge builds a model with the merge variant.
func buildFullBigQuerySinkModel_WithMerge() *BigQuerySinkModel {
	m := buildFullBigQuerySinkModel()
	m.WriteMode = &BigqueryWriteModeModel{
		Merge: &BigqueryMergeModel{
			UseTransactions: types.BoolValue(true),
		},
	}
	return m
}

// buildFullBigQuerySinkModel_WithStorageWriteAPI builds a model with the storage_write_api variant.
func buildFullBigQuerySinkModel_WithStorageWriteAPI() *BigQuerySinkModel {
	m := buildFullBigQuerySinkModel()
	m.WriteMode = &BigqueryWriteModeModel{
		StorageWriteAPI: &BigqueryStorageWriteApiModel{
			MaxStalenessMinutes: types.Int32Value(42),
		},
	}
	return m
}

// buildFullClickhouseSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullClickhouseSinkModel() *ClickhouseSinkModel {
	return &ClickhouseSinkModel{
		AsyncInserts:        types.BoolValue(true),
		DisableSchemaPrefix: types.BoolValue(true),
		Engine:              types.StringValue("Auto"),
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		HTTPURL:                   types.StringValue("v-http_url"),
		MaxSnapshotConcurrency:    types.Int32Value(42),
		NonNullableColumns:        types.BoolValue(true),
		Password:                  types.StringValue("secret-password"),
		PreserveSourceNullability: types.BoolValue(true),
		SslClientCertPEM:          types.StringValue("secret-ssl_client_cert_pem"),
		SslClientKeyPEM:           types.StringValue("secret-ssl_client_key_pem"),
		SslRootCert:               types.StringValue("secret-ssl_root_cert"),
		SslVerify:                 types.BoolValue(true),
		TableNameModifier: &ClickhouseTableNameModifierModel{
			Suffix: &ClickhouseCustomSuffixModel{
				Value: types.StringValue("v-value"),
			},
		},
		TargetDatabase: types.StringValue("v-target_database"),
		TtlDays:        types.Int32Value(42),
		User:           types.StringValue("v-user"),
	}
}

// buildFullClickhouseSinkModel_WithSuffix builds a model with the suffix variant.
func buildFullClickhouseSinkModel_WithSuffix() *ClickhouseSinkModel {
	m := buildFullClickhouseSinkModel()
	m.TableNameModifier = &ClickhouseTableNameModifierModel{
		Suffix: &ClickhouseCustomSuffixModel{
			Value: types.StringValue("v-value"),
		},
	}
	return m
}

// buildFullClickhouseSinkModel_WithPrefix builds a model with the prefix variant.
func buildFullClickhouseSinkModel_WithPrefix() *ClickhouseSinkModel {
	m := buildFullClickhouseSinkModel()
	m.TableNameModifier = &ClickhouseTableNameModifierModel{
		Prefix: &ClickhouseCustomPrefixModel{
			Value: types.StringValue("v-value"),
		},
	}
	return m
}

// buildFullDatabricksSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullDatabricksSinkModel() *DatabricksSinkModel {
	return &DatabricksSinkModel{
		Auth: &DatabricksAuthModel{
			Cli: &DatabricksCliAuthModel{},
		},
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		StorageCredential: types.StringValue("v-storage_credential"),
		TargetCatalog:     types.StringValue("v-target_catalog"),
		TargetSchema:      types.StringValue("v-target_schema"),
		Volume:            types.StringValue("v-volume"),
		Warehouse:         types.StringValue("v-warehouse"),
	}
}

// buildFullDatabricksSinkModel_WithCli builds a model with the cli variant.
func buildFullDatabricksSinkModel_WithCli() *DatabricksSinkModel {
	m := buildFullDatabricksSinkModel()
	m.Auth = &DatabricksAuthModel{
		Cli: &DatabricksCliAuthModel{},
	}
	return m
}

// buildFullDatabricksSinkModel_WithPat builds a model with the pat variant.
func buildFullDatabricksSinkModel_WithPat() *DatabricksSinkModel {
	m := buildFullDatabricksSinkModel()
	m.Auth = &DatabricksAuthModel{
		Pat: &DatabricksPatAuthModel{
			PersonalAccessToken: types.StringValue("secret-personal_access_token"),
			WorkspaceHost:       types.StringValue("v-workspace_host"),
		},
	}
	return m
}

// buildFullDatabricksSinkModel_WithM2m builds a model with the m2m variant.
func buildFullDatabricksSinkModel_WithM2m() *DatabricksSinkModel {
	m := buildFullDatabricksSinkModel()
	m.Auth = &DatabricksAuthModel{
		M2m: &DatabricksM2mAuthModel{
			ClientID:      types.StringValue("v-client_id"),
			ClientSecret:  types.StringValue("secret-client_secret"),
			WorkspaceHost: types.StringValue("v-workspace_host"),
		},
	}
	return m
}

// buildFullDb2SourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullDb2SourceModel() *Db2SourceModel {
	return &Db2SourceModel{
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		Database: types.StringValue("v-database"),
		Host:     types.StringValue("v-host"),
		KeylessTableStrategy: &MigrationKeylessTableStrategyModel{
			AppendOnly: &MigrationAppendOnlyModel{},
		},
		MaxPoolSize: types.Int32Value(42),
		Password:    types.StringValue("secret-password"),
		Port:        types.Int32Value(42),
		ReplicationType: &Db2ReplicationTypeModel{
			Snapshot: &Db2SnapshotModel{
				MaxTextSize: types.Int32Value(42),
			},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullDb2SourceModel_WithAppendOnly builds a model with the append_only variant.
func buildFullDb2SourceModel_WithAppendOnly() *Db2SourceModel {
	m := buildFullDb2SourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		AppendOnly: &MigrationAppendOnlyModel{},
	}
	return m
}

// buildFullDb2SourceModel_WithDedupByRowHash builds a model with the dedup_by_row_hash variant.
func buildFullDb2SourceModel_WithDedupByRowHash() *Db2SourceModel {
	m := buildFullDb2SourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		DedupByRowHash: &MigrationDedupByRowHashModel{},
	}
	return m
}

// buildFullDorisSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullDorisSinkModel() *DorisSinkModel {
	return &DorisSinkModel{
		BinaryHandlingMode:  types.StringValue("Bytes"),
		DisableSchemaPrefix: types.BoolValue(true),
		FeHTTPURL:           types.StringValue("v-fe_http_url"),
		FeMysqlPoolMax:      types.Int32Value(42),
		FeMysqlPort:         types.Int32Value(42),
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		MaxSnapshotConcurrency:    types.Int32Value(42),
		Password:                  types.StringValue("secret-password"),
		PreserveSourceNullability: types.BoolValue(true),
		SslClientCertPEM:          types.StringValue("secret-ssl_client_cert_pem"),
		SslClientKeyPEM:           types.StringValue("secret-ssl_client_key_pem"),
		SslRootCert:               types.StringValue("secret-ssl_root_cert"),
		SslVerify:                 types.BoolValue(true),
		TableModel:                types.StringValue("Auto"),
		TableNameModifier: &DorisTableNameModifierModel{
			Suffix: &DorisCustomSuffixModel{
				Value: types.StringValue("v-value"),
			},
		},
		TargetDatabase: types.StringValue("v-target_database"),
		User:           types.StringValue("v-user"),
	}
}

// buildFullDorisSinkModel_WithSuffix builds a model with the suffix variant.
func buildFullDorisSinkModel_WithSuffix() *DorisSinkModel {
	m := buildFullDorisSinkModel()
	m.TableNameModifier = &DorisTableNameModifierModel{
		Suffix: &DorisCustomSuffixModel{
			Value: types.StringValue("v-value"),
		},
	}
	return m
}

// buildFullDorisSinkModel_WithPrefix builds a model with the prefix variant.
func buildFullDorisSinkModel_WithPrefix() *DorisSinkModel {
	m := buildFullDorisSinkModel()
	m.TableNameModifier = &DorisTableNameModifierModel{
		Prefix: &DorisCustomPrefixModel{
			Value: types.StringValue("v-value"),
		},
	}
	return m
}

// buildFullDuckDBSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullDuckDBSinkModel() *DuckDBSinkModel {
	return &DuckDBSinkModel{
		Connection: &DuckdbConnectionModel{
			Quack: &DuckdbQuackModel{
				Ssl:   types.BoolValue(true),
				Token: types.StringValue("secret-token"),
				URL:   types.StringValue("v-url"),
			},
		},
		EnablePrimaryKeys: types.BoolValue(true),
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		MaxSnapshotConcurrency:    types.Int32Value(42),
		PreserveSourceNullability: types.BoolValue(true),
		TargetDatabase:            types.StringValue("v-target_database"),
		TargetSchema:              types.StringValue("v-target_schema"),
		UseTransactions:           types.BoolValue(true),
	}
}

// buildFullDuckDBSinkModel_WithQuack builds a model with the quack variant.
func buildFullDuckDBSinkModel_WithQuack() *DuckDBSinkModel {
	m := buildFullDuckDBSinkModel()
	m.Connection = &DuckdbConnectionModel{
		Quack: &DuckdbQuackModel{
			Ssl:   types.BoolValue(true),
			Token: types.StringValue("secret-token"),
			URL:   types.StringValue("v-url"),
		},
	}
	return m
}

// buildFullDuckDBSinkModel_WithPg builds a model with the pg variant.
func buildFullDuckDBSinkModel_WithPg() *DuckDBSinkModel {
	m := buildFullDuckDBSinkModel()
	m.Connection = &DuckdbConnectionModel{
		Pg: &PostgresModel{
			Database:                    types.StringValue("v-database"),
			Host:                        types.StringValue("v-host"),
			MaxPoolSize:                 types.Int64Value(42),
			OperationLockTimeoutSeconds: types.Int32Value(42),
			Password:                    types.StringValue("secret-password"),
			Port:                        types.Int32Value(42),
			SslCert:                     types.StringValue("secret-ssl_cert"),
			SslKey:                      types.StringValue("secret-ssl_key"),
			SslMode:                     types.StringValue("Prefer"),
			SslRootCert:                 types.StringValue("secret-ssl_root_cert"),
			Tunnel: &SshTunnelTypeModel{
				Ssh: &SshTunnelModel{
					Auth: &SshAuthModel{
						GeneratedKey: &SshGeneratedKeyModel{
							PrivateKey: types.StringValue("secret-private_key"),
							PublicKey:  types.StringValue("v-public_key"),
						},
					},
					BastionAlternates: types.ListNull(types.StringType),
					BastionHost:       types.StringValue("v-bastion_host"),
					BastionPort:       types.Int32Value(42),
					User:              types.StringValue("v-user"),
				},
			},
			User: types.StringValue("v-user"),
		},
	}
	return m
}

// buildFullFileSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullFileSourceModel() *FileSourceModel {
	return &FileSourceModel{
		Discovery: &FileDiscoveryModel{
			Poll: &FilePollModel{
				IntervalSeconds: types.Int64Value(42),
			},
		},
		ExcludePatterns: types.ListNull(types.StringType),
		FormatDetails: &BufferFormatDetailsModel{
			Csv: &BufferCsvFormatModel{
				AllowJaggedRows: types.BoolValue(true),
				Comment:         types.StringValue("v-comment"),
				Delimiter:       types.StringValue("v-delimiter"),
				Encoding:        types.StringValue("v-encoding"),
				Escape:          types.StringValue("v-escape"),
				HasHeader:       types.BoolValue(true),
				NullValues:      types.ListNull(types.StringType),
				Quote:           types.StringValue("v-quote"),
				SkipRows:        types.Int32Value(42),
				Terminator:      types.StringValue("v-terminator"),
			},
		},
		GlobPatterns: types.ListNull(types.StringType),
		ObjectStore: &BufferObjectStoreModel{
			AllowHTTP:                types.BoolValue(true),
			AllowInvalidCertificates: types.BoolValue(true),
			MaxConcurrentParts:       types.Int32Value(42),
			MaxConcurrentRequests:    types.Int32Value(42),
			Options: map[string]*BufferOptionModel{
				"k1": {
					Value: types.StringValue("v-value"),
				},
			},
			RootCertificatePEM: types.StringValue("secret-root_certificate_pem"),
			URL:                types.StringValue("v-url"),
		},
		PostProcessing: &FilePostProcessingModel{
			Delete: &FileDeleteAfterProcessingModel{},
		},
		StartDate: types.Int64Value(42),
		TableMapping: &FileTableMappingModel{
			Single: &FileSingleTableMappingModel{
				Destination: types.StringValue("v-destination"),
			},
		},
	}
}

// buildFullFileSourceModel_WithCsv builds a model with the csv variant.
func buildFullFileSourceModel_WithCsv() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.FormatDetails = &BufferFormatDetailsModel{
		Csv: &BufferCsvFormatModel{
			AllowJaggedRows: types.BoolValue(true),
			Comment:         types.StringValue("v-comment"),
			Delimiter:       types.StringValue("v-delimiter"),
			Encoding:        types.StringValue("v-encoding"),
			Escape:          types.StringValue("v-escape"),
			HasHeader:       types.BoolValue(true),
			NullValues:      types.ListNull(types.StringType),
			Quote:           types.StringValue("v-quote"),
			SkipRows:        types.Int32Value(42),
			Terminator:      types.StringValue("v-terminator"),
		},
	}
	return m
}

// buildFullFileSourceModel_WithJSON builds a model with the json variant.
func buildFullFileSourceModel_WithJSON() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.FormatDetails = &BufferFormatDetailsModel{
		JSON: &BufferJsonFormatModel{
			Flatten:         types.BoolValue(true),
			MultiLine:       types.BoolValue(true),
			StripOuterArray: types.BoolValue(true),
		},
	}
	return m
}

// buildFullFileSourceModel_WithParquet builds a model with the parquet variant.
func buildFullFileSourceModel_WithParquet() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.FormatDetails = &BufferFormatDetailsModel{
		Parquet: &BufferParquetFormatModel{
			BinaryAsText: types.BoolValue(true),
		},
	}
	return m
}

// buildFullFileSourceModel_WithDelete builds a model with the delete variant.
func buildFullFileSourceModel_WithDelete() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.PostProcessing = &FilePostProcessingModel{
		Delete: &FileDeleteAfterProcessingModel{},
	}
	return m
}

// buildFullFileSourceModel_WithMove builds a model with the move variant.
func buildFullFileSourceModel_WithMove() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.PostProcessing = &FilePostProcessingModel{
		Move: &FileMoveAfterProcessingModel{
			Destination: types.StringValue("v-destination"),
		},
	}
	return m
}

// buildFullFileSourceModel_WithSingle builds a model with the single variant.
func buildFullFileSourceModel_WithSingle() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.TableMapping = &FileTableMappingModel{
		Single: &FileSingleTableMappingModel{
			Destination: types.StringValue("v-destination"),
		},
	}
	return m
}

// buildFullFileSourceModel_WithAuto builds a model with the auto variant.
func buildFullFileSourceModel_WithAuto() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.TableMapping = &FileTableMappingModel{
		Auto: &FileAutoTableMappingModel{
			Prefix: types.StringValue("v-prefix"),
			Suffix: types.StringValue("v-suffix"),
		},
	}
	return m
}

// buildFullFileSourceModel_WithDynamic builds a model with the dynamic variant.
func buildFullFileSourceModel_WithDynamic() *FileSourceModel {
	m := buildFullFileSourceModel()
	m.TableMapping = &FileTableMappingModel{
		Dynamic: &FileDynamicTableMappingModel{
			Pattern:  types.StringValue("v-pattern"),
			Template: types.StringValue("v-template"),
		},
	}
	return m
}

// buildFullIcebergSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullIcebergSinkModel() *IcebergSinkModel {
	return &IcebergSinkModel{
		Catalog: &IcebergCatalogModel{
			Rest: &IcebergRestCatalogModel{
				Auth: &IcebergCatalogAuthModel{
					Oauth2: &IcebergOAuth2AuthModel{
						ClientID:      types.StringValue("v-client_id"),
						ClientSecret:  types.StringValue("secret-client_secret"),
						Scope:         types.StringValue("v-scope"),
						TokenEndpoint: types.StringValue("v-token_endpoint"),
					},
				},
				URI:       types.StringValue("v-uri"),
				Warehouse: types.StringValue("v-warehouse"),
			},
		},
		MaxCatalogConcurrency: types.Int32Value(42),
		MetadataCompression:   types.StringValue("Gzip"),
		SpecVersion:           types.StringValue("V1"),
		StorageCredentials: &IcebergStorageCredentialsModel{
			S3: &IcebergS3CredentialsModel{
				AccessKeyID:     types.StringValue("v-access_key_id"),
				Endpoint:        types.StringValue("v-endpoint"),
				IamRoleArn:      types.StringValue("v-iam_role_arn"),
				PathStyleAccess: types.BoolValue(true),
				ProfileName:     types.StringValue("v-profile_name"),
				Region:          types.StringValue("v-region"),
				SecretAccessKey: types.StringValue("secret-secret_access_key"),
				SessionToken:    types.StringValue("secret-session_token"),
			},
		},
		TargetNamespace:       types.ListNull(types.StringType),
		TruncateTableIfExists: types.BoolValue(true),
		VendedCredentials:     types.BoolValue(true),
		WriteMode: &IcebergWriteModeModel{
			Append: &IcebergAppendModel{},
		},
	}
}

// buildFullIcebergSinkModel_WithRest builds a model with the rest variant.
func buildFullIcebergSinkModel_WithRest() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Rest: &IcebergRestCatalogModel{
			Auth: &IcebergCatalogAuthModel{
				Oauth2: &IcebergOAuth2AuthModel{
					ClientID:      types.StringValue("v-client_id"),
					ClientSecret:  types.StringValue("secret-client_secret"),
					Scope:         types.StringValue("v-scope"),
					TokenEndpoint: types.StringValue("v-token_endpoint"),
				},
			},
			URI:       types.StringValue("v-uri"),
			Warehouse: types.StringValue("v-warehouse"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithGlue builds a model with the glue variant.
func buildFullIcebergSinkModel_WithGlue() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Glue: &IcebergGlueCatalogModel{
			AccessKeyID:     types.StringValue("secret-access_key_id"),
			CatalogID:       types.StringValue("v-catalog_id"),
			ProfileName:     types.StringValue("v-profile_name"),
			Region:          types.StringValue("v-region"),
			SecretAccessKey: types.StringValue("secret-secret_access_key"),
			SessionToken:    types.StringValue("secret-session_token"),
			URI:             types.StringValue("v-uri"),
			Warehouse:       types.StringValue("v-warehouse"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithHms builds a model with the hms variant.
func buildFullIcebergSinkModel_WithHms() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Hms: &IcebergHmsCatalogModel{
			ThriftTransport: types.StringValue("v-thrift_transport"),
			URI:             types.StringValue("v-uri"),
			Warehouse:       types.StringValue("v-warehouse"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithS3tables builds a model with the s3tables variant.
func buildFullIcebergSinkModel_WithS3tables() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		S3tables: &IcebergS3TablesCatalogModel{
			AccessKeyID:     types.StringValue("v-access_key_id"),
			Endpoint:        types.StringValue("v-endpoint"),
			ProfileName:     types.StringValue("v-profile_name"),
			Region:          types.StringValue("v-region"),
			SecretAccessKey: types.StringValue("secret-secret_access_key"),
			SessionToken:    types.StringValue("secret-session_token"),
			TableBucketArn:  types.StringValue("v-table_bucket_arn"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithSql builds a model with the sql variant.
func buildFullIcebergSinkModel_WithSql() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Sql: &IcebergSqlCatalogModel{
			BindStyle: types.StringValue("v-bind_style"),
			URI:       types.StringValue("v-uri"),
			Warehouse: types.StringValue("v-warehouse"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithSnowflake builds a model with the snowflake variant.
func buildFullIcebergSinkModel_WithSnowflake() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Snowflake: &IcebergSnowflakeCatalogModel{
			Database:  types.StringValue("v-database"),
			Password:  types.StringValue("secret-password"),
			Role:      types.StringValue("v-role"),
			Schema:    types.StringValue("v-schema"),
			URI:       types.StringValue("v-uri"),
			Username:  types.StringValue("v-username"),
			Warehouse: types.StringValue("v-warehouse"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithUnity builds a model with the unity variant.
func buildFullIcebergSinkModel_WithUnity() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.Catalog = &IcebergCatalogModel{
		Unity: &IcebergUnityCatalogModel{
			CatalogName: types.StringValue("v-catalog_name"),
			Token:       types.StringValue("secret-token"),
			URI:         types.StringValue("v-uri"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithS3 builds a model with the s3 variant.
func buildFullIcebergSinkModel_WithS3() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.StorageCredentials = &IcebergStorageCredentialsModel{
		S3: &IcebergS3CredentialsModel{
			AccessKeyID:     types.StringValue("v-access_key_id"),
			Endpoint:        types.StringValue("v-endpoint"),
			IamRoleArn:      types.StringValue("v-iam_role_arn"),
			PathStyleAccess: types.BoolValue(true),
			ProfileName:     types.StringValue("v-profile_name"),
			Region:          types.StringValue("v-region"),
			SecretAccessKey: types.StringValue("secret-secret_access_key"),
			SessionToken:    types.StringValue("secret-session_token"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithGcs builds a model with the gcs variant.
func buildFullIcebergSinkModel_WithGcs() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.StorageCredentials = &IcebergStorageCredentialsModel{
		Gcs: &IcebergGcsCredentialsModel{
			CredentialsJSON: types.StringValue("secret-credentials_json"),
			ProjectID:       types.StringValue("v-project_id"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithAzure builds a model with the azure variant.
func buildFullIcebergSinkModel_WithAzure() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.StorageCredentials = &IcebergStorageCredentialsModel{
		Azure: &IcebergAzureCredentialsModel{
			AccountKey:   types.StringValue("secret-account_key"),
			AccountName:  types.StringValue("v-account_name"),
			ClientID:     types.StringValue("v-client_id"),
			ClientSecret: types.StringValue("secret-client_secret"),
			Endpoint:     types.StringValue("v-endpoint"),
			TenantID:     types.StringValue("v-tenant_id"),
		},
	}
	return m
}

// buildFullIcebergSinkModel_WithCustom builds a model with the custom variant.
func buildFullIcebergSinkModel_WithCustom() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.StorageCredentials = &IcebergStorageCredentialsModel{
		Custom: &IcebergCustomCredentialsModel{},
	}
	return m
}

// buildFullIcebergSinkModel_WithAppend builds a model with the append variant.
func buildFullIcebergSinkModel_WithAppend() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.WriteMode = &IcebergWriteModeModel{
		Append: &IcebergAppendModel{},
	}
	return m
}

// buildFullIcebergSinkModel_WithMergeOnRead builds a model with the merge_on_read variant.
func buildFullIcebergSinkModel_WithMergeOnRead() *IcebergSinkModel {
	m := buildFullIcebergSinkModel()
	m.WriteMode = &IcebergWriteModeModel{
		MergeOnRead: &IcebergMergeOnReadModel{
			DeleteMode: types.StringValue("Soft"),
			HistoryMode: &MigrationHistoryModeModel{
				Append: &MigrationAppendHistoryModel{
					Suffix: types.StringValue("v-suffix"),
				},
			},
			UsePositionalDeletesOnly: types.BoolValue(true),
		},
	}
	return m
}

// buildFullKafkaSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullKafkaSinkModel() *KafkaSinkModel {
	return &KafkaSinkModel{
		Connection: &KafkaModel{
			Brokers: types.ListNull(types.StringType),
		},
		Format: &KaformatModel{
			Debezium: &KafkaDebeziumFormatModel{
				EmitTxMetadata: types.BoolValue(true),
				FormatConfig: &KaformatConfigModel{
					KeySerde: &KafkaSerDeModel{
						JSON: &KafkaJsonModel{
							SchemaRegistry: &KafkaSchemaRegistryModel{
								Confluent: &KafkaConfluentModel{
									Password: types.StringValue("secret-password"),
									URL:      types.StringValue("v-url"),
									Username: types.StringValue("v-username"),
								},
							},
						},
					},
					ValueSerde: &KafkaSerDeModel{
						JSON: &KafkaJsonModel{
							SchemaRegistry: &KafkaSchemaRegistryModel{
								Confluent: &KafkaConfluentModel{
									Password: types.StringValue("secret-password"),
									URL:      types.StringValue("v-url"),
									Username: types.StringValue("v-username"),
								},
							},
						},
					},
				},
				IncludeBefore:      types.BoolValue(true),
				SkippedOperations:  types.StringValue("v-skipped_operations"),
				TombstonesOnDelete: types.BoolValue(true),
				TxMetadataTopic:    types.StringValue("v-tx_metadata_topic"),
			},
		},
		Name:              types.StringValue("v-name"),
		TopicNameTemplate: types.StringValue("v-topic_name_template"),
		TopicOptions: &KafkaTopicOptionsModel{
			NamingMode:         types.StringValue("Default"),
			OperationTimeoutMs: types.Int32Value(42),
			Partitions:         types.Int32Value(42),
			ReplicationFactor:  types.Int32Value(42),
		},
	}
}

// buildFullKafkaSinkModel_WithDebezium builds a model with the debezium variant.
func buildFullKafkaSinkModel_WithDebezium() *KafkaSinkModel {
	m := buildFullKafkaSinkModel()
	m.Format = &KaformatModel{
		Debezium: &KafkaDebeziumFormatModel{
			EmitTxMetadata: types.BoolValue(true),
			FormatConfig: &KaformatConfigModel{
				KeySerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
				ValueSerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
			},
			IncludeBefore:      types.BoolValue(true),
			SkippedOperations:  types.StringValue("v-skipped_operations"),
			TombstonesOnDelete: types.BoolValue(true),
			TxMetadataTopic:    types.StringValue("v-tx_metadata_topic"),
		},
	}
	return m
}

// buildFullKafkaSinkModel_WithSupermetal builds a model with the supermetal variant.
func buildFullKafkaSinkModel_WithSupermetal() *KafkaSinkModel {
	m := buildFullKafkaSinkModel()
	m.Format = &KaformatModel{
		Supermetal: &KafkaSupermetalFormatModel{
			FormatConfig: &KaformatConfigModel{
				KeySerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
				ValueSerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
			},
			TombstonesOnDelete: types.BoolValue(true),
		},
	}
	return m
}

// buildFullMongoSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullMongoSourceModel() *MongoSourceModel {
	return &MongoSourceModel{
		Address:              types.StringValue("v-address"),
		AuthenticationSource: types.StringValue("v-authentication_source"),
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		Database:        types.StringValue("v-database"),
		FlattenBody:     types.BoolValue(true),
		FlattenMaxDepth: types.Int32Value(42),
		MaxPoolSize:     types.Int32Value(42),
		Password:        types.StringValue("secret-password"),
		ReplicationType: &MongoReplicationTypeModel{
			ChangeStreams: &MongoChangeStreamsModel{
				ReplicationMode: &MongoReplicationModeModel{
					SchemaMode: &MongoSchemaModeModel{
						InferTypedStrings:     types.BoolValue(true),
						MajorityTypeThreshold: types.Float64Value(42.0),
						ObjectStore: &BufferObjectStoreModel{
							AllowHTTP:                types.BoolValue(true),
							AllowInvalidCertificates: types.BoolValue(true),
							MaxConcurrentParts:       types.Int32Value(42),
							MaxConcurrentRequests:    types.Int32Value(42),
							Options: map[string]*BufferOptionModel{
								"k1": {
									Value: types.StringValue("v-value"),
								},
							},
							RootCertificatePEM: types.StringValue("secret-root_certificate_pem"),
							URL:                types.StringValue("v-url"),
						},
					},
				},
				SkipSnapshot: types.BoolValue(true),
			},
		},
		SslMode: &MongoSslModeModel{
			Disable: &MongoDisableModel{},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullMongoSourceModel_WithDisable builds a model with the disable variant.
func buildFullMongoSourceModel_WithDisable() *MongoSourceModel {
	m := buildFullMongoSourceModel()
	m.SslMode = &MongoSslModeModel{
		Disable: &MongoDisableModel{},
	}
	return m
}

// buildFullMongoSourceModel_WithEnable builds a model with the enable variant.
func buildFullMongoSourceModel_WithEnable() *MongoSourceModel {
	m := buildFullMongoSourceModel()
	m.SslMode = &MongoSslModeModel{
		Enable: &MongoEnableModel{
			CAFile:      types.StringValue("secret-ca_file"),
			CertKeyFile: types.StringValue("secret-cert_key_file"),
		},
	}
	return m
}

// buildFullMongoSourceModel_WithChangeStreams builds a model with the change_streams variant.
func buildFullMongoSourceModel_WithChangeStreams() *MongoSourceModel {
	m := buildFullMongoSourceModel()
	m.ReplicationType = &MongoReplicationTypeModel{
		ChangeStreams: &MongoChangeStreamsModel{
			ReplicationMode: &MongoReplicationModeModel{
				SchemaMode: &MongoSchemaModeModel{
					InferTypedStrings:     types.BoolValue(true),
					MajorityTypeThreshold: types.Float64Value(42.0),
					ObjectStore: &BufferObjectStoreModel{
						AllowHTTP:                types.BoolValue(true),
						AllowInvalidCertificates: types.BoolValue(true),
						MaxConcurrentParts:       types.Int32Value(42),
						MaxConcurrentRequests:    types.Int32Value(42),
						Options: map[string]*BufferOptionModel{
							"k1": {
								Value: types.StringValue("v-value"),
							},
						},
						RootCertificatePEM: types.StringValue("secret-root_certificate_pem"),
						URL:                types.StringValue("v-url"),
					},
				},
			},
			SkipSnapshot: types.BoolValue(true),
		},
	}
	return m
}

// buildFullMongoSourceModel_WithSnapshot builds a model with the snapshot variant.
func buildFullMongoSourceModel_WithSnapshot() *MongoSourceModel {
	m := buildFullMongoSourceModel()
	m.ReplicationType = &MongoReplicationTypeModel{
		Snapshot: &MongoSnapshotModel{
			ReplicationMode: &MongoReplicationModeModel{
				SchemaMode: &MongoSchemaModeModel{
					InferTypedStrings:     types.BoolValue(true),
					MajorityTypeThreshold: types.Float64Value(42.0),
					ObjectStore: &BufferObjectStoreModel{
						AllowHTTP:                types.BoolValue(true),
						AllowInvalidCertificates: types.BoolValue(true),
						MaxConcurrentParts:       types.Int32Value(42),
						MaxConcurrentRequests:    types.Int32Value(42),
						Options: map[string]*BufferOptionModel{
							"k1": {
								Value: types.StringValue("v-value"),
							},
						},
						RootCertificatePEM: types.StringValue("secret-root_certificate_pem"),
						URL:                types.StringValue("v-url"),
					},
				},
			},
		},
	}
	return m
}

// buildFullMySqlSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullMySqlSourceModel() *MySqlSourceModel {
	return &MySqlSourceModel{
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		Database:               types.StringValue("v-database"),
		Host:                   types.StringValue("v-host"),
		InferTinyint1AsBoolean: types.BoolValue(true),
		KeylessTableStrategy: &MigrationKeylessTableStrategyModel{
			AppendOnly: &MigrationAppendOnlyModel{},
		},
		MaxPoolSize:              types.Int32Value(42),
		ParallelSnapshotsEnabled: types.BoolValue(true),
		Password:                 types.StringValue("secret-password"),
		Port:                     types.Int32Value(42),
		SkipSnapshot:             types.BoolValue(true),
		SslMode: &MysqlSslModeModel{
			Disable: &MysqlDisableModel{},
		},
		Tunnel: &SshTunnelTypeModel{
			Ssh: &SshTunnelModel{
				Auth: &SshAuthModel{
					BringYourOwnKey: &SshBringYourOwnKeyModel{
						PrivateKey: types.StringValue("secret-private_key"),
					},
				},
				BastionAlternates: types.ListNull(types.StringType),
				BastionHost:       types.StringValue("v-bastion_host"),
				BastionPort:       types.Int32Value(42),
				User:              types.StringValue("v-user"),
			},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullMySqlSourceModel_WithDisable builds a model with the disable variant.
func buildFullMySqlSourceModel_WithDisable() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.SslMode = &MysqlSslModeModel{
		Disable: &MysqlDisableModel{},
	}
	return m
}

// buildFullMySqlSourceModel_WithRequire builds a model with the require variant.
func buildFullMySqlSourceModel_WithRequire() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.SslMode = &MysqlSslModeModel{
		Require: &MysqlRequireModel{},
	}
	return m
}

// buildFullMySqlSourceModel_WithVerifyCA builds a model with the verify_ca variant.
func buildFullMySqlSourceModel_WithVerifyCA() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.SslMode = &MysqlSslModeModel{
		VerifyCA: &MysqlVerifyCaModel{
			CAFile: types.StringValue("secret-ca_file"),
		},
	}
	return m
}

// buildFullMySqlSourceModel_WithVerifyIdentity builds a model with the verify_identity variant.
func buildFullMySqlSourceModel_WithVerifyIdentity() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.SslMode = &MysqlSslModeModel{
		VerifyIdentity: &MysqlVerifyIdentityModel{
			CAFile: types.StringValue("secret-ca_file"),
		},
	}
	return m
}

// buildFullMySqlSourceModel_WithAppendOnly builds a model with the append_only variant.
func buildFullMySqlSourceModel_WithAppendOnly() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		AppendOnly: &MigrationAppendOnlyModel{},
	}
	return m
}

// buildFullMySqlSourceModel_WithDedupByRowHash builds a model with the dedup_by_row_hash variant.
func buildFullMySqlSourceModel_WithDedupByRowHash() *MySqlSourceModel {
	m := buildFullMySqlSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		DedupByRowHash: &MigrationDedupByRowHashModel{},
	}
	return m
}

// buildFullOracleSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullOracleSourceModel() *OracleSourceModel {
	return &OracleSourceModel{
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		ConnectString: types.StringValue("v-connect_string"),
		KeylessTableStrategy: &MigrationKeylessTableStrategyModel{
			AppendOnly: &MigrationAppendOnlyModel{},
		},
		MaxPoolSize:              types.Int32Value(42),
		ParallelSnapshotsEnabled: types.BoolValue(true),
		Password:                 types.StringValue("secret-password"),
		Pdb:                      types.StringValue("v-pdb"),
		ReplicationType: &OracleReplicationTypeModel{
			LogMiner: &OracleLogMinerModel{
				CommittedDataOnly:        types.BoolValue(true),
				Dict:                     types.StringValue("OnlineCatalog"),
				MaxBufferMemorySizePerTx: types.Int64Value(42),
				PollIntervalSecs:         types.Int64Value(42),
			},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullOracleSourceModel_WithAppendOnly builds a model with the append_only variant.
func buildFullOracleSourceModel_WithAppendOnly() *OracleSourceModel {
	m := buildFullOracleSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		AppendOnly: &MigrationAppendOnlyModel{},
	}
	return m
}

// buildFullOracleSourceModel_WithDedupByRowHash builds a model with the dedup_by_row_hash variant.
func buildFullOracleSourceModel_WithDedupByRowHash() *OracleSourceModel {
	m := buildFullOracleSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		DedupByRowHash: &MigrationDedupByRowHashModel{},
	}
	return m
}

// buildFullPostgresSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullPostgresSourceModel() *PostgresSourceModel {
	return &PostgresSourceModel{
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		Database: types.StringValue("v-database"),
		Host:     types.StringValue("v-host"),
		KeylessTableStrategy: &MigrationKeylessTableStrategyModel{
			AppendOnly: &MigrationAppendOnlyModel{},
		},
		MaxPoolSize:                 types.Int64Value(42),
		OperationLockTimeoutSeconds: types.Int32Value(42),
		ParallelSnapshotsEnabled:    types.BoolValue(true),
		PartitionsAsRoot:            types.BoolValue(true),
		Password:                    types.StringValue("secret-password"),
		Port:                        types.Int32Value(42),
		ReplicationType: &PostgresReplicationTypeModel{
			LogicalReplication: &PostgresLogicalReplicationModel{
				PublicationName:    types.StringValue("v-publication_name"),
				RetryWindowSeconds: types.Int64Value(42),
				SkipSnapshots:      types.BoolValue(true),
			},
		},
		SslCert:     types.StringValue("secret-ssl_cert"),
		SslKey:      types.StringValue("secret-ssl_key"),
		SslMode:     types.StringValue("Prefer"),
		SslRootCert: types.StringValue("secret-ssl_root_cert"),
		Tunnel: &SshTunnelTypeModel{
			Ssh: &SshTunnelModel{
				Auth: &SshAuthModel{
					BringYourOwnKey: &SshBringYourOwnKeyModel{
						PrivateKey: types.StringValue("secret-private_key"),
					},
				},
				BastionAlternates: types.ListNull(types.StringType),
				BastionHost:       types.StringValue("v-bastion_host"),
				BastionPort:       types.Int32Value(42),
				User:              types.StringValue("v-user"),
			},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullPostgresSourceModel_WithAppendOnly builds a model with the append_only variant.
func buildFullPostgresSourceModel_WithAppendOnly() *PostgresSourceModel {
	m := buildFullPostgresSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		AppendOnly: &MigrationAppendOnlyModel{},
	}
	return m
}

// buildFullPostgresSourceModel_WithDedupByRowHash builds a model with the dedup_by_row_hash variant.
func buildFullPostgresSourceModel_WithDedupByRowHash() *PostgresSourceModel {
	m := buildFullPostgresSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		DedupByRowHash: &MigrationDedupByRowHashModel{},
	}
	return m
}

// buildFullPostgresSourceModel_WithLogicalReplication builds a model with the logical_replication variant.
func buildFullPostgresSourceModel_WithLogicalReplication() *PostgresSourceModel {
	m := buildFullPostgresSourceModel()
	m.ReplicationType = &PostgresReplicationTypeModel{
		LogicalReplication: &PostgresLogicalReplicationModel{
			PublicationName:    types.StringValue("v-publication_name"),
			RetryWindowSeconds: types.Int64Value(42),
			SkipSnapshots:      types.BoolValue(true),
		},
	}
	return m
}

// buildFullPostgresSourceModel_WithSnapshot builds a model with the snapshot variant.
func buildFullPostgresSourceModel_WithSnapshot() *PostgresSourceModel {
	m := buildFullPostgresSourceModel()
	m.ReplicationType = &PostgresReplicationTypeModel{
		Snapshot: &PostgresSnapshotModel{},
	}
	return m
}

// buildFullPostgresSourceModel_WithQueryBased builds a model with the query_based variant.
func buildFullPostgresSourceModel_WithQueryBased() *PostgresSourceModel {
	m := buildFullPostgresSourceModel()
	m.ReplicationType = &PostgresReplicationTypeModel{
		QueryBased: &PostgresQueryBasedModel{
			DeleteTracking: &PostgresDeleteTrackingModel{
				Enabled: &PostgresDeleteTrackingConfigModel{
					PkIndexStore: &BufferObjectStoreModel{
						AllowHTTP:                types.BoolValue(true),
						AllowInvalidCertificates: types.BoolValue(true),
						MaxConcurrentParts:       types.Int32Value(42),
						MaxConcurrentRequests:    types.Int32Value(42),
						Options: map[string]*BufferOptionModel{
							"k1": {
								Value: types.StringValue("v-value"),
							},
						},
						RootCertificatePEM: types.StringValue("secret-root_certificate_pem"),
						URL:                types.StringValue("v-url"),
					},
					PollIntervalSeconds: types.Int32Value(42),
				},
			},
			MaxSkipCycles:       types.Int32Value(42),
			PollIntervalSeconds: types.Int32Value(42),
		},
	}
	return m
}

// buildFullPostgresSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullPostgresSinkModel() *PostgresSinkModel {
	return &PostgresSinkModel{
		Database:                    types.StringValue("v-database"),
		Host:                        types.StringValue("v-host"),
		MaxPoolSize:                 types.Int64Value(42),
		OperationLockTimeoutSeconds: types.Int32Value(42),
		Password:                    types.StringValue("secret-password"),
		Port:                        types.Int32Value(42),
		SslCert:                     types.StringValue("secret-ssl_cert"),
		SslKey:                      types.StringValue("secret-ssl_key"),
		SslMode:                     types.StringValue("Prefer"),
		SslRootCert:                 types.StringValue("secret-ssl_root_cert"),
		TargetSchema:                types.StringValue("v-target_schema"),
		Tunnel: &SshTunnelTypeModel{
			Ssh: &SshTunnelModel{
				Auth: &SshAuthModel{
					GeneratedKey: &SshGeneratedKeyModel{
						PrivateKey: types.StringValue("secret-private_key"),
						PublicKey:  types.StringValue("v-public_key"),
					},
				},
				BastionAlternates: types.ListNull(types.StringType),
				BastionHost:       types.StringValue("v-bastion_host"),
				BastionPort:       types.Int32Value(42),
				User:              types.StringValue("v-user"),
			},
		},
		User: types.StringValue("v-user"),
	}
}

// buildFullSnowflakeSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullSnowflakeSinkModel() *SnowflakeSinkModel {
	return &SnowflakeSinkModel{
		AccountIdentifier: types.StringValue("v-account_identifier"),
		Auth: &SnowflakeAuthModel{
			Password: &SnowflakePasswordModel{
				Password: types.StringValue("secret-password"),
			},
		},
		HistoryMode: &MigrationHistoryModeModel{
			Append: &MigrationAppendHistoryModel{
				Suffix: types.StringValue("v-suffix"),
			},
		},
		Role:            types.StringValue("v-role"),
		TargetDatabase:  types.StringValue("v-target_database"),
		TargetSchema:    types.StringValue("v-target_schema"),
		UseTransactions: types.BoolValue(true),
		User:            types.StringValue("v-user"),
		Warehouse:       types.StringValue("v-warehouse"),
	}
}

// buildFullSnowflakeSinkModel_WithPassword builds a model with the password variant.
func buildFullSnowflakeSinkModel_WithPassword() *SnowflakeSinkModel {
	m := buildFullSnowflakeSinkModel()
	m.Auth = &SnowflakeAuthModel{
		Password: &SnowflakePasswordModel{
			Password: types.StringValue("secret-password"),
		},
	}
	return m
}

// buildFullSnowflakeSinkModel_WithKeyPair builds a model with the key_pair variant.
func buildFullSnowflakeSinkModel_WithKeyPair() *SnowflakeSinkModel {
	m := buildFullSnowflakeSinkModel()
	m.Auth = &SnowflakeAuthModel{
		KeyPair: &SnowflakeKeyPairModel{
			PrivateKeyPassword: types.StringValue("secret-private_key_password"),
			PrivateKeyPEM:      types.StringValue("secret-private_key_pem"),
		},
	}
	return m
}

// buildFullSqlServerSourceModel builds a fully-populated model with deterministic synthetic values.
func buildFullSqlServerSourceModel() *SqlServerSourceModel {
	return &SqlServerSourceModel{
		Auth: &SqlServerAuthModel{
			Standard: &SqlServerStandardAuthModel{
				Password: types.StringValue("secret-password"),
				User:     types.StringValue("v-user"),
			},
		},
		Catalog: &CatalogModel{
			DefaultAction: types.StringValue("Include"),
			Name:          types.StringValue("v-name"),
			Schemas: map[string]*CatalogSchemaModel{
				"k1": {
					Action: types.StringValue("Include"),
					Tables: map[string]*CatalogTableModel{
						"k1": {
							Action: types.StringValue("Include"),
							Columns: map[string]*CatalogColumnModel{
								"k1": {
									Action: types.StringValue("Include"),
								},
							},
						},
					},
				},
			},
		},
		Database: types.StringValue("v-database"),
		Host:     types.StringValue("v-host"),
		KeylessTableStrategy: &MigrationKeylessTableStrategyModel{
			AppendOnly: &MigrationAppendOnlyModel{},
		},
		MaxPoolSize:              types.Int32Value(42),
		ParallelSnapshotsEnabled: types.BoolValue(true),
		Port:                     types.Int32Value(42),
		ReplicationType: &SqlServerReplicationTypeModel{
			CaptureTableReplication: &SqlServerCaptureTableReplicationModel{
				CdcMaxScans:             types.Int32Value(42),
				CdcMaxTrans:             types.Int32Value(42),
				CdcPollIntervalSecs:     types.Int64Value(42),
				SkipSnapshot:            types.BoolValue(true),
				SkipTablesWithoutCt:     types.BoolValue(true),
				TransactionalCdcEnabled: types.BoolValue(true),
			},
		},
		SnapshotIsolation: types.StringValue("Required"),
		SslMode: &SqlServerSslModeModel{
			Disable: &SqlServerDisableModel{},
		},
	}
}

// buildFullSqlServerSourceModel_WithStandard builds a model with the standard variant.
func buildFullSqlServerSourceModel_WithStandard() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.Auth = &SqlServerAuthModel{
		Standard: &SqlServerStandardAuthModel{
			Password: types.StringValue("secret-password"),
			User:     types.StringValue("v-user"),
		},
	}
	return m
}

// buildFullSqlServerSourceModel_WithWindows builds a model with the windows variant.
func buildFullSqlServerSourceModel_WithWindows() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.Auth = &SqlServerAuthModel{
		Windows: &SqlServerWindowsAuthModel{
			Password: types.StringValue("secret-password"),
			User:     types.StringValue("v-user"),
		},
	}
	return m
}

// buildFullSqlServerSourceModel_WithActivedirectory builds a model with the activedirectory variant.
func buildFullSqlServerSourceModel_WithActivedirectory() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.Auth = &SqlServerAuthModel{
		Activedirectory: &SqlServerAdAuthModel{
			Token: types.StringValue("secret-token"),
		},
	}
	return m
}

// buildFullSqlServerSourceModel_WithDisable builds a model with the disable variant.
func buildFullSqlServerSourceModel_WithDisable() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.SslMode = &SqlServerSslModeModel{
		Disable: &SqlServerDisableModel{},
	}
	return m
}

// buildFullSqlServerSourceModel_WithEnable builds a model with the enable variant.
func buildFullSqlServerSourceModel_WithEnable() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.SslMode = &SqlServerSslModeModel{
		Enable: &SqlServerEnableModel{
			CAFile: types.StringValue("secret-ca_file"),
		},
	}
	return m
}

// buildFullSqlServerSourceModel_WithAppendOnly builds a model with the append_only variant.
func buildFullSqlServerSourceModel_WithAppendOnly() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		AppendOnly: &MigrationAppendOnlyModel{},
	}
	return m
}

// buildFullSqlServerSourceModel_WithDedupByRowHash builds a model with the dedup_by_row_hash variant.
func buildFullSqlServerSourceModel_WithDedupByRowHash() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.KeylessTableStrategy = &MigrationKeylessTableStrategyModel{
		DedupByRowHash: &MigrationDedupByRowHashModel{},
	}
	return m
}

// buildFullSqlServerSourceModel_WithCaptureTableReplication builds a model with the capture_table_replication variant.
func buildFullSqlServerSourceModel_WithCaptureTableReplication() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.ReplicationType = &SqlServerReplicationTypeModel{
		CaptureTableReplication: &SqlServerCaptureTableReplicationModel{
			CdcMaxScans:             types.Int32Value(42),
			CdcMaxTrans:             types.Int32Value(42),
			CdcPollIntervalSecs:     types.Int64Value(42),
			SkipSnapshot:            types.BoolValue(true),
			SkipTablesWithoutCt:     types.BoolValue(true),
			TransactionalCdcEnabled: types.BoolValue(true),
		},
	}
	return m
}

// buildFullSqlServerSourceModel_WithSnapshot builds a model with the snapshot variant.
func buildFullSqlServerSourceModel_WithSnapshot() *SqlServerSourceModel {
	m := buildFullSqlServerSourceModel()
	m.ReplicationType = &SqlServerReplicationTypeModel{
		Snapshot: &SqlServerSnapshotModel{},
	}
	return m
}

// buildFullWebhookSinkModel builds a fully-populated model with deterministic synthetic values.
func buildFullWebhookSinkModel() *WebhookSinkModel {
	return &WebhookSinkModel{
		Auth: &WebhookAuthModel{
			Bearer: &WebhookBearerAuthModel{
				Token: types.StringValue("secret-token"),
			},
		},
		Compression: types.StringValue("None"),
		Format: &KaformatModel{
			Debezium: &KafkaDebeziumFormatModel{
				EmitTxMetadata: types.BoolValue(true),
				FormatConfig: &KaformatConfigModel{
					KeySerde: &KafkaSerDeModel{
						JSON: &KafkaJsonModel{
							SchemaRegistry: &KafkaSchemaRegistryModel{
								Confluent: &KafkaConfluentModel{
									Password: types.StringValue("secret-password"),
									URL:      types.StringValue("v-url"),
									Username: types.StringValue("v-username"),
								},
							},
						},
					},
					ValueSerde: &KafkaSerDeModel{
						JSON: &KafkaJsonModel{
							SchemaRegistry: &KafkaSchemaRegistryModel{
								Confluent: &KafkaConfluentModel{
									Password: types.StringValue("secret-password"),
									URL:      types.StringValue("v-url"),
									Username: types.StringValue("v-username"),
								},
							},
						},
					},
				},
				IncludeBefore:      types.BoolValue(true),
				SkippedOperations:  types.StringValue("v-skipped_operations"),
				TombstonesOnDelete: types.BoolValue(true),
				TxMetadataTopic:    types.StringValue("v-tx_metadata_topic"),
			},
		},
		PathTemplate:     types.StringValue("v-path_template"),
		RequestTimeoutMs: types.Int32Value(42),
		URL:              types.StringValue("v-url"),
	}
}

// buildFullWebhookSinkModel_WithBearer builds a model with the bearer variant.
func buildFullWebhookSinkModel_WithBearer() *WebhookSinkModel {
	m := buildFullWebhookSinkModel()
	m.Auth = &WebhookAuthModel{
		Bearer: &WebhookBearerAuthModel{
			Token: types.StringValue("secret-token"),
		},
	}
	return m
}

// buildFullWebhookSinkModel_WithBasic builds a model with the basic variant.
func buildFullWebhookSinkModel_WithBasic() *WebhookSinkModel {
	m := buildFullWebhookSinkModel()
	m.Auth = &WebhookAuthModel{
		Basic: &WebhookBasicAuthModel{
			Password: types.StringValue("secret-password"),
			Username: types.StringValue("v-username"),
		},
	}
	return m
}

// buildFullWebhookSinkModel_WithHeader builds a model with the header variant.
func buildFullWebhookSinkModel_WithHeader() *WebhookSinkModel {
	m := buildFullWebhookSinkModel()
	m.Auth = &WebhookAuthModel{
		Header: &WebhookHeaderAuthModel{
			Name:  types.StringValue("v-name"),
			Value: types.StringValue("secret-value"),
		},
	}
	return m
}

// buildFullWebhookSinkModel_WithDebezium builds a model with the debezium variant.
func buildFullWebhookSinkModel_WithDebezium() *WebhookSinkModel {
	m := buildFullWebhookSinkModel()
	m.Format = &KaformatModel{
		Debezium: &KafkaDebeziumFormatModel{
			EmitTxMetadata: types.BoolValue(true),
			FormatConfig: &KaformatConfigModel{
				KeySerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
				ValueSerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
			},
			IncludeBefore:      types.BoolValue(true),
			SkippedOperations:  types.StringValue("v-skipped_operations"),
			TombstonesOnDelete: types.BoolValue(true),
			TxMetadataTopic:    types.StringValue("v-tx_metadata_topic"),
		},
	}
	return m
}

// buildFullWebhookSinkModel_WithSupermetal builds a model with the supermetal variant.
func buildFullWebhookSinkModel_WithSupermetal() *WebhookSinkModel {
	m := buildFullWebhookSinkModel()
	m.Format = &KaformatModel{
		Supermetal: &KafkaSupermetalFormatModel{
			FormatConfig: &KaformatConfigModel{
				KeySerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
				ValueSerde: &KafkaSerDeModel{
					JSON: &KafkaJsonModel{
						SchemaRegistry: &KafkaSchemaRegistryModel{
							Confluent: &KafkaConfluentModel{
								Password: types.StringValue("secret-password"),
								URL:      types.StringValue("v-url"),
								Username: types.StringValue("v-username"),
							},
						},
					},
				},
			},
			TombstonesOnDelete: types.BoolValue(true),
		},
	}
	return m
}

func TestRoundTrip_BigQuerySink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullBigQuerySinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &BigQuerySinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullBigQuerySinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &BigQuerySinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_merge", func(t *testing.T) {
		full := buildFullBigQuerySinkModel_WithMerge()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &BigQuerySinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_storage_write_api", func(t *testing.T) {
		full := buildFullBigQuerySinkModel_WithStorageWriteAPI()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &BigQuerySinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_ClickhouseSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullClickhouseSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &ClickhouseSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullClickhouseSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &ClickhouseSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_suffix", func(t *testing.T) {
		full := buildFullClickhouseSinkModel_WithSuffix()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &ClickhouseSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_prefix", func(t *testing.T) {
		full := buildFullClickhouseSinkModel_WithPrefix()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &ClickhouseSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_DatabricksSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullDatabricksSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DatabricksSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullDatabricksSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &DatabricksSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_cli", func(t *testing.T) {
		full := buildFullDatabricksSinkModel_WithCli()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DatabricksSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_pat", func(t *testing.T) {
		full := buildFullDatabricksSinkModel_WithPat()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DatabricksSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_m2m", func(t *testing.T) {
		full := buildFullDatabricksSinkModel_WithM2m()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DatabricksSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_Db2Source(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullDb2SourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &Db2SourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullDb2SourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &Db2SourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append_only", func(t *testing.T) {
		full := buildFullDb2SourceModel_WithAppendOnly()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &Db2SourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dedup_by_row_hash", func(t *testing.T) {
		full := buildFullDb2SourceModel_WithDedupByRowHash()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &Db2SourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_DorisSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullDorisSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DorisSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullDorisSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &DorisSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_suffix", func(t *testing.T) {
		full := buildFullDorisSinkModel_WithSuffix()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DorisSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_prefix", func(t *testing.T) {
		full := buildFullDorisSinkModel_WithPrefix()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DorisSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_DuckDBSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullDuckDBSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DuckDBSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullDuckDBSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &DuckDBSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_quack", func(t *testing.T) {
		full := buildFullDuckDBSinkModel_WithQuack()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DuckDBSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_pg", func(t *testing.T) {
		full := buildFullDuckDBSinkModel_WithPg()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &DuckDBSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_FileSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullFileSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullFileSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_csv", func(t *testing.T) {
		full := buildFullFileSourceModel_WithCsv()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_json", func(t *testing.T) {
		full := buildFullFileSourceModel_WithJSON()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_parquet", func(t *testing.T) {
		full := buildFullFileSourceModel_WithParquet()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_delete", func(t *testing.T) {
		full := buildFullFileSourceModel_WithDelete()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_move", func(t *testing.T) {
		full := buildFullFileSourceModel_WithMove()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_single", func(t *testing.T) {
		full := buildFullFileSourceModel_WithSingle()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_auto", func(t *testing.T) {
		full := buildFullFileSourceModel_WithAuto()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dynamic", func(t *testing.T) {
		full := buildFullFileSourceModel_WithDynamic()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &FileSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_IcebergSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullIcebergSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullIcebergSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_rest", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithRest()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_glue", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithGlue()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_hms", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithHms()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_s3tables", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithS3tables()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_sql", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithSql()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_snowflake", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithSnowflake()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_unity", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithUnity()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_s3", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithS3()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_gcs", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithGcs()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_azure", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithAzure()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_custom", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithCustom()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithAppend()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_merge_on_read", func(t *testing.T) {
		full := buildFullIcebergSinkModel_WithMergeOnRead()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &IcebergSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_KafkaSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullKafkaSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &KafkaSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullKafkaSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &KafkaSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_debezium", func(t *testing.T) {
		full := buildFullKafkaSinkModel_WithDebezium()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &KafkaSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_supermetal", func(t *testing.T) {
		full := buildFullKafkaSinkModel_WithSupermetal()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &KafkaSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_MongoSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullMongoSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullMongoSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_disable", func(t *testing.T) {
		full := buildFullMongoSourceModel_WithDisable()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_enable", func(t *testing.T) {
		full := buildFullMongoSourceModel_WithEnable()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_change_streams", func(t *testing.T) {
		full := buildFullMongoSourceModel_WithChangeStreams()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_snapshot", func(t *testing.T) {
		full := buildFullMongoSourceModel_WithSnapshot()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MongoSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_MySqlSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullMySqlSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullMySqlSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_disable", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithDisable()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_require", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithRequire()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_verify_ca", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithVerifyCA()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_verify_identity", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithVerifyIdentity()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append_only", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithAppendOnly()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dedup_by_row_hash", func(t *testing.T) {
		full := buildFullMySqlSourceModel_WithDedupByRowHash()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &MySqlSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_OracleSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullOracleSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &OracleSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullOracleSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &OracleSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append_only", func(t *testing.T) {
		full := buildFullOracleSourceModel_WithAppendOnly()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &OracleSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dedup_by_row_hash", func(t *testing.T) {
		full := buildFullOracleSourceModel_WithDedupByRowHash()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &OracleSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_PostgresSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullPostgresSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullPostgresSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append_only", func(t *testing.T) {
		full := buildFullPostgresSourceModel_WithAppendOnly()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dedup_by_row_hash", func(t *testing.T) {
		full := buildFullPostgresSourceModel_WithDedupByRowHash()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_logical_replication", func(t *testing.T) {
		full := buildFullPostgresSourceModel_WithLogicalReplication()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_snapshot", func(t *testing.T) {
		full := buildFullPostgresSourceModel_WithSnapshot()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_query_based", func(t *testing.T) {
		full := buildFullPostgresSourceModel_WithQueryBased()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_PostgresSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullPostgresSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &PostgresSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullPostgresSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &PostgresSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_SnowflakeSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullSnowflakeSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SnowflakeSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullSnowflakeSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &SnowflakeSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_password", func(t *testing.T) {
		full := buildFullSnowflakeSinkModel_WithPassword()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SnowflakeSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_key_pair", func(t *testing.T) {
		full := buildFullSnowflakeSinkModel_WithKeyPair()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SnowflakeSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_SqlServerSource(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullSqlServerSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullSqlServerSourceModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_standard", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithStandard()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_windows", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithWindows()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_activedirectory", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithActivedirectory()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_disable", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithDisable()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_enable", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithEnable()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_append_only", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithAppendOnly()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_dedup_by_row_hash", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithDedupByRowHash()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_capture_table_replication", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithCaptureTableReplication()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_snapshot", func(t *testing.T) {
		full := buildFullSqlServerSourceModel_WithSnapshot()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &SqlServerSourceModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}

func TestRoundTrip_WebhookSink(t *testing.T) {
	t.Run("full_round_trip", func(t *testing.T) {
		full := buildFullWebhookSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("redaction_preserves_secrets", func(t *testing.T) {
		full := buildFullWebhookSinkModel()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		// Simulate API redaction: clear all secret fields
		redactSecrets(&apiObj)

		// Merge with full model as state - secrets should be preserved from state
		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("redaction round-trip mismatch - secrets not preserved (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_bearer", func(t *testing.T) {
		full := buildFullWebhookSinkModel_WithBearer()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_basic", func(t *testing.T) {
		full := buildFullWebhookSinkModel_WithBasic()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_header", func(t *testing.T) {
		full := buildFullWebhookSinkModel_WithHeader()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_debezium", func(t *testing.T) {
		full := buildFullWebhookSinkModel_WithDebezium()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

	t.Run("variant_supermetal", func(t *testing.T) {
		full := buildFullWebhookSinkModel_WithSupermetal()
		apiObj, diags := full.toAPI()
		if diags.HasError() {
			t.Fatalf("toAPI failed: %v", diags)
		}

		result := &WebhookSinkModel{}
		result.mergeFromAPI(apiObj, full)

		if diff := cmp.Diff(full, result, cmp.Exporter(func(reflect.Type) bool { return true })); diff != "" {
			t.Errorf("variant round-trip mismatch (-want +got):\n%s", diff)
		}
	})

}
